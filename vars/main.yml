dependencies:
  - gnupg

base_hashicorp_url: https://apt.releases.hashicorp.com
gpg_key_url: "{{ base_hashicorp_url }}/gpg"
gpg_key_file_armored: /tmp/packer.gpg
gpg_key_file: /etc/apt/keyrings/packer.gpg
